---
services:
  paychecks:
    image: docker.io/visionir/paychecks:latest
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /paychecks
    volumes:
    - ./src:/paychecks
    - ./pyproject.toml:/paychecks/pyproject.toml
    ports:
    - 8501:8501
